'use strict';

export enum ResourceType {
    Message = "epinio:message",
    Applications = "epinio:applications",
    Application = "epinio:application",
    Services = "epinio:services",
    Service = "epinio:service"
}
